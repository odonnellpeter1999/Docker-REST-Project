# Docker quoco REST service

To run project use the `docker-compose up` then use mvn exec:java -pl client to run the client code.

Note: make sure you are in the root directory while running the project.

